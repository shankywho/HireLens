# ==============================================================================
# HireLens Frontend Environment Configuration Template (.env.example)
# Copy this file to frontend/.env.local (or frontend/.env) and adjust as needed.
# ==============================================================================

# HireLens Express Backend REST API Base URL
# Default local Express server runs on port 3001
VITE_API_BASE_URL="http://localhost:3001/api"
